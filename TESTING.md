# 🧪 Руководство по тестированию игры

## Быстрый тест

### Открыть игру в браузере

**Linux:**
```bash
cd "/home/rom/ПРоектики/Игра про танчики 2.0"
xdg-open index.html
```

**Или через Python:**
```bash
cd "/home/rom/ПРоектики/Игра про танчики 2.0"
python3 -m http.server 8000
# Затем откройте http://localhost:8000 в браузере
```

## Чек-лист функций

### ✅ Главное меню
- [ ] Отображается заголовок "3D ТАНЧИКИ"
- [ ] Видны 3 карточки танков (легкий, средний, тяжелый)
- [ ] При наведении карточки подсвечиваются
- [ ] Кнопки "Выбрать" кликабельны
- [ ] Видна инфор мация об управлении

### ✅ Игровой процесс
- [ ] После выбора танка открывается игровая арена
- [ ] Видна 3D сцена с танком игрока
- [ ] HUD отображается корректно (здоровье, очки, волна, враги)
- [ ] Танк управляется WASD
- [ ] Башня поворачивается за мышью
- [ ] Стрельба работает (Пробел/ЛКМ)
- [ ] Появляются враги
- [ ] Враги двигаются и стреляют
- [ ] Снаряды летят и наносят урон
- [ ] При попадании появляются эффекты взрыва
- [ ] Звуки воспроизводятся

### ✅ Улучшения
- [ ] После уничтожения врагов иногда появляются улучшения
- [ ] Улучшения вращаются и левитируют
- [ ] При подборе показывается сообщение
- [ ] Эффекты улучшений работают

### ✅ Система волн
- [ ] После уничтожения всех врагов начинается новая волна
- [ ] Показывается сообщение "ВОЛНА X"
- [ ] С каждой волной врагов становится больше
- [ ] На высоких волнах появляются более сильные враги

### ✅ Game Over
- [ ] При смерти игрока показывается экран Game Over
- [ ] Отображается статистика (счет, волна, убийства)
- [ ] Сохраняется лучший результат
- [ ] Кнопки "Играть снова" и "Главное меню" работают

### ✅ Пауза
- [ ] ESC открывает меню паузы
- [ ] Игра останавливается
- [ ] Можно продолжить или выйти в меню

## Тест производительности

### Проверка FPS

Откройте консоль браузера (F12) и добавьте счетчик FPS:

```javascript
let lastTime = performance.now();
let frames = 0;
function checkFPS() {
    frames++;
    const now = performance.now();
    if (now >= lastTime + 1000) {
        console.log(`FPS: ${frames}`);
        frames = 0;
        lastTime = now;
    }
    requestAnimationFrame(checkFPS);
}
checkFPS();
```

**Целевые значения:**
- 60 FPS - отлично
- 30-60 FPS - хорошо
- 20-30 FPS - приемлемо
- <20 FPS - нужна оптимизация

### Если FPS низкий

Откройте `js/game.js` и измените настройки рендера:

```javascript
// Текущие настройки (строка ~45)
this.renderer.setPixelRatio(Math.min(window.devicePixelRatio, 1.5));

// Попробуйте снизить до:
this.renderer.setPixelRatio(1);
```

Или в `js/arena.js` уменьшите количество препятствий:

```javascript
// Текущее (строка ~58)
const obstacleCount = 8;

// Попробуйте:
const obstacleCount = 4;
```

## Тест звуков

1. Проверьте, что звуки воспроизводятся:
   - Звук выстрела (при стрельбе)
   - Звук взрыва (при попадании в врага)
   - Звук попадания (когда враг попал в вас)
   - Звук улучшения (при подборе)
   - Звук Game Over

2. Если звуки не работают:
   - Проверьте настройки звука в браузере
   - Некоторые браузеры блокируют autoplay звуков
   - Кликните на странице перед игрой (активация Audio Context)

## Тест столкновений

### Стены
- [ ] Танк не проходит сквозь стены арены
- [ ] Враги не проходят сквозь стены

### Препятствия
- [ ] Танк останавливается у препятствий
- [ ] Враги объезжают препятствия

### Снаряды
- [ ] Снаряды игрока попадают во врагов
- [ ] Снаряды врагов попадают в игрока
- [ ] Снаряды не проходят сквозь цели

## Тест балансировки

### Легкий танк
- Должен быть быстрым и маневренным
- Умирает за 4-5 попаданий
- Хорош для уклонения

### Средний танк
- Универсален
- Умирает за 5-6 попаданий
- Хорош для новичков

### Тяжелый танк
- Медленный но живучий
- Умирает за 7-8 попаданий
- Большой урон

### Враги
- **Волна 1-2:** Легко убиваются (2-3 выстрела)
- **Волна 3-5:** Средняя сложность
- **Волна 6+:** Сложно (много врагов)

## Тест совместимости браузеров

### Chrome / Chromium ✅
Лучшая производительность и совместимость

### Firefox ✅
Хорошая производительность

### Edge ✅
Хорошая производительность (на Chromium)

### Safari ⚠️
Может потребоваться включить WebGL в настройках

## Известные проблемы и решения

### Черный экран при запуске
**Причина:** WebGL не поддерживается или отключен
**Решение:** 
1. Обновите драйверы видеокарты
2. Включите аппаратное ускорение в браузере
3. Попробуйте другой браузер

### Низкий FPS
**Причина:** Слабое железо
**Решение:**
1. Закройте другие вкладки браузера
2. Уменьшите разрешение окна браузера
3. Измените настройки рендера (см. выше)

### Не работают звуки
**Причина:** Audio Context не активирован
**Решение:**
1. Кликните на странице перед началом игры
2. Проверьте настройки звука браузера
3. Убедитесь, что звук не заблокирован

### Танк "прилипает" к стенам
**Причина:** Недостаточная проверка столкновений
**Решение:** Это нормальное поведение, отойдите в сторону

### Враги "застревают"
**Причина:** AI пытается пройти сквозь препятствие
**Решение:** AI автоматически сменит направление через 3 секунды

## Отладка

### Включить отладочную информацию

В консоли браузера (F12):

```javascript
// Показать позицию игрока
setInterval(() => {
    if (window.game && window.game.player) {
        console.log('Player position:', window.game.player.group.position);
    }
}, 1000);

// Показать количество объектов
setInterval(() => {
    if (window.game) {
        console.log('Enemies:', window.game.enemies.length);
        console.log('Projectiles:', window.game.projectiles.length);
        console.log('Powerups:', window.game.powerups.length);
    }
}, 1000);
```

### Проверить производительность рендера

```javascript
console.log('Renderer info:', window.game.renderer.info);
```

## Стресс-тест

Для проверки производительности под нагрузкой, измените максимальное количество врагов:

```javascript
// В game.js, функция startWave() (строка ~119)
this.enemiesInWave = Math.min(this.enemiesInWave, 10); // Изменить на 20

// ВНИМАНИЕ: Это может сильно нагрузить систему!
```

## Автоматическое тестирование (опционально)

Для продвинутых пользователей - можно добавить автоматизированные тесты:

```bash
npm install -g jest puppeteer
```

Но для простой игры ручное тестирование вполне достаточно.

---

**Рекомендуется протестировать все пункты перед распространением игры!**

Если нашли баги, запишите:
1. Шаги для воспроизведения
2. Ожидаемое поведение
3. Фактическое поведение
4. Скриншоты/видео (если возможно)

