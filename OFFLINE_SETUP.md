# 📦 Настройка для полностью OFFLINE работы

По умолчанию игра использует Three.js с CDN, что требует интернет при первой загрузке. Если вы хотите сделать игру **полностью автономной**, следуйте этим инструкциям.

## Вариант 1: Скачать Three.js локально (Рекомендуется)

### Шаг 1: Скачать Three.js

1. Создайте папку `libs` в корне проекта:
```bash
mkdir libs
```

2. Скачайте Three.js:
   - Перейдите на https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js
   - Сохраните файл как `libs/three.min.js`

Или через командную строку:
```bash
cd "Игра про танчики 2.0"
mkdir -p libs
curl -o libs/three.min.js https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js
```

### Шаг 2: Изменить index.html

Откройте `index.html` и замените строку:
```html
<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
```

На:
```html
<script src="libs/three.min.js"></script>
```

### Готово!

Теперь игра полностью автономна и будет работать без интернета.

## Вариант 2: Использовать кэш браузера

При первом запуске игры с интернетом, браузер кэширует Three.js. После этого игра будет работать offline без дополнительных действий.

**Проверка кэша:**
1. Откройте игру с интернетом один раз
2. Отключите интернет
3. Попробуйте открыть игру снова
4. Если работает - кэш активен, дополнительные действия не нужны

## Вариант 3: Service Worker (продвинутый)

Для автоматического кэширования можно добавить Service Worker:

Создайте файл `sw.js` в корне:
```javascript
const CACHE_NAME = 'tank-game-v1';
const urlsToCache = [
  '/',
  '/index.html',
  '/css/style.css',
  '/js/main.js',
  '/js/game.js',
  '/js/tank.js',
  '/js/enemy.js',
  '/js/projectile.js',
  '/js/controls.js',
  '/js/arena.js',
  '/js/powerups.js',
  '/js/ui.js',
  '/js/sound.js',
  'https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js'
];

self.addEventListener('install', event => {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then(cache => cache.addAll(urlsToCache))
  );
});

self.addEventListener('fetch', event => {
  event.respondWith(
    caches.match(event.request)
      .then(response => response || fetch(event.request))
  );
});
```

И добавьте в `index.html` перед закрывающим `</body>`:
```html
<script>
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.register('/sw.js');
}
</script>
```

## Проверка offline работы

1. Откройте игру в браузере
2. Откройте DevTools (F12)
3. Перейдите во вкладку "Network" / "Сеть"
4. Выберите "Offline" в dropdown
5. Перезагрузите страницу (Ctrl+R)
6. Игра должна работать

## Размер файлов

- **Three.js:** ~600 KB (минифицированный)
- **Игровые файлы:** ~50 KB
- **HTML + CSS:** ~25 KB
- **Итого:** ~675 KB

Весь проект легко помещается на USB-флешку или может быть отправлен по email.

## Распространение игры

Для распространения игры как offline приложения:

1. Используйте **Вариант 1** (локальный Three.js)
2. Заархивируйте всю папку проекта
3. Получатель просто распаковывает и открывает `index.html`

**ZIP архив будет весить около 200-250 KB** (благодаря сжатию).

---

💡 **Совет:** Если нужна максимальная совместимость, рекомендуется Вариант 1 с локальным Three.js.

